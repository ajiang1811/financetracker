import { LuzmoDashboard } from '../models/dashboard.model';
import { ItemData } from '../models/common.model';
export declare const getData: <T extends LuzmoDashboard>(dashboardComponent: T, iframe: HTMLIFrameElement, itemId: string) => Promise<ItemData>;
