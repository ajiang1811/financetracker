import { DashboardEditMode, SetEditMode } from '../models/common.model';
import { LuzmoDashboard } from '../models/dashboard.model';
export declare const setEditMode: <T extends LuzmoDashboard>(dashboardComponent: T, iframe: HTMLIFrameElement, editMode?: DashboardEditMode) => Promise<SetEditMode>;
