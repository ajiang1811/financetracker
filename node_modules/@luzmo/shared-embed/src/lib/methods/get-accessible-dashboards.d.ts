import { AccessibleDashboard } from '../models/common.model';
import { LuzmoDashboard } from '../models/dashboard.model';
export declare const getAccessibleDashboards: <T extends LuzmoDashboard>(dashboardComponent: T) => Promise<AccessibleDashboard[]>;
