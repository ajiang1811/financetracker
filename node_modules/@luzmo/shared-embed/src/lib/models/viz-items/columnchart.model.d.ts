import { AvailableSlots, FilterExpression, FilterIn, FilterOperators, ParametersInput } from '../filters.model';
type ColumnchartFilterIn = FilterIn & {
    expression: Extract<FilterExpression, '? between ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'y-axis'>;
        },
        ParametersInput
    ];
};
type ColumnchartFilterIn2 = FilterIn & {
    expression: Extract<FilterExpression, '? in ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'y-axis'>;
        },
        string[]
    ];
};
type ColumnchartFilterInput1 = Partial<Record<FilterOperators, (ColumnchartFilterIn | ColumnchartFilterInput1)[]>>;
type ColumnchartFilterInput2 = Partial<Record<FilterOperators, (ColumnchartFilterIn2 | ColumnchartFilterInput2)[]>>;
export type ColumnChartFilterInput = ColumnchartFilterInput1 | ColumnchartFilterInput2;
export {};
