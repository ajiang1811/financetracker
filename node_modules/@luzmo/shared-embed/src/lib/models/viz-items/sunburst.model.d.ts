import { AvailableSlots, FilterExpression, FilterIn, FilterOperators, ParametersInput } from '../filters.model';
type SunburstFilterIn = FilterIn & {
    expression: Extract<FilterExpression, '? in ?' | '? between ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'levels'>;
            level: number;
        },
        ParametersInput
    ];
};
export type SunburstFilterInput = Partial<Record<FilterOperators, (SunburstFilterIn | SunburstFilterInput)[]>>;
export {};
