import { AvailableSlots, FilterExpression, FilterIn, FilterOperators, ParametersInput } from '../filters.model';
type SelectboxFilterIn = FilterIn & {
    expression: Extract<FilterExpression, '? between ?' | '? in ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'dimension'>;
        },
        ParametersInput
    ];
};
export type SelectboxFilterInput = Partial<Record<FilterOperators, (SelectboxFilterIn | SelectboxFilterInput)[]>>;
export {};
