import { AvailableSlots, FilterExpression, FilterIn, FilterOperators, StringOrNumber } from '../filters.model';
type ScatterplotFilterIn = FilterIn & {
    expression: Extract<FilterExpression, '? >= ?' | '? < ?' | '? in ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'x-axis' | 'y-axis'>;
        },
        StringOrNumber[]
    ];
};
export type ScatterplotFilterInput = Partial<Record<FilterOperators, (ScatterplotFilterIn | ScatterplotFilterInput)[]>>;
export {};
