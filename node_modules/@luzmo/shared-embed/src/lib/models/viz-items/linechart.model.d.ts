import { AvailableSlots, FilterExpression, FilterIn, FilterOperators, StringOrNumber } from '../filters.model';
type LinechartFilterIn = FilterIn & {
    expression: Extract<FilterExpression, '? >= ?' | '? < ?' | '? in ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'x-axis'>;
        },
        StringOrNumber[]
    ];
};
type LinechartFilterIn2 = FilterIn & {
    expression: Extract<FilterExpression, '? >= ?' | '? < ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'x-axis'>;
        },
        [
            StringOrNumber
        ]
    ];
};
type LinechartFilterIn3 = FilterIn & {
    expression: Extract<FilterExpression, '? in ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'x-axis'>;
        },
        string[]
    ];
};
type LinechartFilterInput1 = Partial<Record<FilterOperators, (LinechartFilterIn | LinechartFilterInput1)[]>>;
type LinechartFilterInput2 = Partial<Record<FilterOperators, (LinechartFilterIn2 | LinechartFilterInput2)[]>>;
type LinechartFilterInput3 = Partial<Record<FilterOperators, (LinechartFilterIn3 | LinechartFilterInput3)[]>>;
export type LinechartFilterInput = LinechartFilterInput1 | LinechartFilterInput2 | LinechartFilterInput3;
export {};
