import { AvailableSlots, FilterExpression, FilterIn, FilterOperators } from '../filters.model';
type StripPlotFilterIn = FilterIn & {
    expression: Extract<FilterExpression, '? = ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'y-axis'>;
        },
        [
            string
        ]
    ];
};
export type StripPlotFilterInput = Partial<Record<FilterOperators, (StripPlotFilterIn | StripPlotFilterInput)[]>>;
export {};
