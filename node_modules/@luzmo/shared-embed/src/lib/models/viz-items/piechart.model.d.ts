import { AvailableSlots, FilterExpression, FilterIn, FilterOperators, ParametersInput, StringOrNumber } from '../filters.model';
type PiechartFilterIn = FilterIn & {
    expression: Extract<FilterExpression, '? between ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'category'>;
        },
        ParametersInput
    ];
};
type PiechartFilterIn2 = FilterIn & {
    expression: Extract<FilterExpression, '? in ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'category'>;
        },
        StringOrNumber[]
    ];
};
type PiechartFilterInput1 = Partial<Record<FilterOperators, (PiechartFilterIn | PiechartFilterInput1)[]>>;
type PiechartFilterInput2 = Partial<Record<FilterOperators, (PiechartFilterIn2 | PiechartFilterInput2)[]>>;
export type PieChartFilterInput = PiechartFilterInput1 | PiechartFilterInput2;
export {};
