import { AvailableSlots, FilterExpression, FilterIn, FilterOperators, StringOrNumber } from '../filters.model';
type ParallelCoordinatesFilterIn = FilterIn & {
    expression: Extract<FilterExpression, '? >= ?' | '? < ?' | '? in ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'coordinates'>;
            level: number;
        },
        StringOrNumber[]
    ];
};
export type ParallelCoordinatesFilterInput = Partial<Record<FilterOperators, (ParallelCoordinatesFilterIn | ParallelCoordinatesFilterInput)[]>>;
export {};
