import { AvailableSlots, FilterExpression, FilterIn, FilterOperators, StringOrNumber } from '../filters.model';
type SliderFilterIn = FilterIn & {
    expression: Extract<FilterExpression, '? >= ?' | '? < ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'slidermetric'>;
        },
        StringOrNumber[]
    ];
};
export type SliderFilterInput = Partial<Record<FilterOperators, (SliderFilterIn | SliderFilterInput)[]>>;
export {};
