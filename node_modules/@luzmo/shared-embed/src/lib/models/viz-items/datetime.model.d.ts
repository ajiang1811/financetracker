import { AvailableSlots, FilterExpression, FilterIn, FilterOperators } from '../filters.model';
type DatetimeFilterIn = FilterIn & {
    expression: Extract<FilterExpression, '? >= ?' | '? < ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'time'>;
        },
        string[]
    ];
};
export type DatetimeFilterInput = Partial<Record<FilterOperators, (DatetimeFilterIn | DatetimeFilterInput)[]>>;
export {};
