import { AvailableSlots, FilterExpression, FilterIn, FilterOperators, StringOrNumber } from '../filters.model';
type ChoroplethFilterIn = FilterIn & {
    expression: Extract<FilterExpression, '? in ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'geo'>;
        },
        StringOrNumber[]
    ];
};
export type ChoroplethFilterInput = Partial<Record<FilterOperators, (ChoroplethFilterIn | ChoroplethFilterInput)[]>>;
export {};
