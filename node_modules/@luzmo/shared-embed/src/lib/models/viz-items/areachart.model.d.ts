import { AvailableSlots, FilterExpression, FilterIn, FilterOperators, ParametersInput } from '../filters.model';
type AreaChartFilterIn = FilterIn & {
    expression: Extract<FilterExpression, '? in ?' | '? >= ?' | '? <= ?' | '? < ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'x-axis'>;
        },
        ParametersInput
    ];
};
export type AreaChartFilterInput = Partial<Record<FilterOperators, (AreaChartFilterIn | AreaChartFilterInput)[]>>;
export {};
