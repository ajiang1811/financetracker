import { AvailableSlots, FilterExpression, FilterIn, FilterOperators, ParametersInput } from '../filters.model';
type TreemapFilterIn = FilterIn & {
    expression: Extract<FilterExpression, '? between ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'category'>;
        },
        ParametersInput
    ];
};
type TreemapFilterIn2 = FilterIn & {
    expression: Extract<FilterExpression, '? in ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'category'>;
        },
        string[]
    ];
};
type TreemapFilterInput1 = Partial<Record<FilterOperators, (TreemapFilterIn | TreemapFilterInput1)[]>>;
type TreemapFilterInput2 = Partial<Record<FilterOperators, (TreemapFilterIn2 | TreemapFilterInput2)[]>>;
export type TreemapFilterInput = TreemapFilterInput1 | TreemapFilterInput2;
export {};
