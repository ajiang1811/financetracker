import { AvailableSlots, FilterExpression, FilterIn, FilterOperators, ParametersInput } from '../filters.model';
type BarchartFilterIn = FilterIn & {
    expression: Extract<FilterExpression, '? between ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'y-axis'>;
        },
        ParametersInput
    ];
};
type BarchartFilterIn2 = FilterIn & {
    expression: Extract<FilterExpression, '? in ?'>;
    parameters: [
        {
            slot: Extract<AvailableSlots, 'y-axis'>;
        },
        string[]
    ];
};
type BarchartFilterInput1 = Partial<Record<FilterOperators, (BarchartFilterIn | BarchartFilterInput1)[]>>;
type BarchartFilterInput2 = Partial<Record<FilterOperators, (BarchartFilterIn2 | BarchartFilterInput2)[]>>;
export type BarChartFilterInput = BarchartFilterInput1 | BarchartFilterInput2;
export {};
